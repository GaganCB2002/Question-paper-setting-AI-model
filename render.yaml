services:
  - type: web
    name: question-paper-setting-ai-model
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 4
    rootDir: backend
