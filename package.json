{
  "name": "kke-question-paper-generator",
  "version": "1.0.0",
  "description": "Karnataka Government Exam Question Paper Generator powered by Gemini AI",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && set USE_SQLITE=1 && set PYTHONPATH=. && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
